<h1><%= @path %></h1>
<ul>
  <% if archives.length > 0 %>
    <% for entry in archives %>
      <li>
        <a href="<%= entry.path %>"><%= entry.title %></a> - <span class="archive-date"><%= entry.date %></span>
      </li>
    <% end %>
  <% end %>
</ul>

